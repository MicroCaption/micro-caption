# Vendored CUDA libs are large binaries fetched out-of-band (see fetch_cudnn.sh)
cudnn/
!fetch_cudnn.sh
!.gitignore

# mediamtx RTMP server binary (fetched out-of-band — see scratch/fetch_mediamtx.sh)
mediamtx/
